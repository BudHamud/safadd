{
  "installCommand": "cd .. && pnpm install --filter ./web... --no-frozen-lockfile",
  "buildCommand": "pnpm build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [
    { "path": "/api/cron/refresh-rates", "schedule": "0 5 * * *" }
  ]
}
