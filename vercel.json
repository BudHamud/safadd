{
  "installCommand": "pnpm install --filter web...",
  "buildCommand": "pnpm --filter web exec prisma generate && pnpm --filter web build",
  "outputDirectory": "web/.next",
  "framework": "nextjs"
}