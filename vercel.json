{
  "installCommand": "pnpm install --filter ./web... --frozen-lockfile --prefer-offline --ignore-scripts --reporter=append-only",
  "buildCommand": "pnpm --dir web exec prisma generate && pnpm --dir web build",
  "outputDirectory": "web/.next",
  "framework": "nextjs"
}