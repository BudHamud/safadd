// Re-exported from the shared package — single source of truth.
export {
  getLocale,
  formatAmount,
  formatCurrency,
  formatMonthLabel,
  parseDateValue,
  formatShortDate,
  formatLongDate,
} from '@safed/shared/locale';
